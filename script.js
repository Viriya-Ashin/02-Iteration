// Define an array of student names
var students = ["Zaw Mg", "Zin Min", "Zaw Htet", "Su Myat", "Su Lat"];
// Output the number of students in the array
console.log(students.length);
// Iterate through the array and greet each student
for (var i = 0; i < students.length; i = i + 1){
    console.log("Great to see you, " + students[i] + "!");
};